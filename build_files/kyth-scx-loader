#!/usr/bin/env bash
set -euo pipefail
# Rust owns the ^scx_[a-z0-9_]+$ validation and the "leaving sched_ext unset"
# refusal/skip contract for a missing configuration.
exec /usr/bin/kyth-runtime scx-loader "$@"
