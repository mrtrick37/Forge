# shellcheck shell=bash
# ── Gaming save cloud (restic local + rclone) ────────────────────────────
# kyth-save-sync is the native Rust binary copied from the
# hub-web-builder stage; no Python launcher remains in the source tree.
# save-cloud.toml + restic repo /var/cache/kyth/saves + rclone remote hash-gated
