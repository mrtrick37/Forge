# shellcheck shell=bash
# ── QuickSettings deep (Win+A + brightness) ──────────────────────────────
# kyth-apply-quicksettings is the native Rust binary copied from the
# hub-web-builder stage; no Python launcher remains in the source tree.
# quicksettings.toml hash-gated powerdevil brightness
