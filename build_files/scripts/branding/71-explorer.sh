# shellcheck shell=bash
# ── Explorer parity (Dolphin double-click + preview + drives) ────────────
# kyth-apply-explorer is the native Rust binary copied from the
# hub-web-builder stage; no Python launcher remains in the source tree.
# explorer.toml hash-gated kdeglobals SingleClick
