# shellcheck shell=bash
# ── Dev containers preset ────────────────────────────────────────────────
# kyth-setup-devcontainer is the native Rust binary copied from the
# hub-web-builder stage; no Python launcher remains in the source tree.
# devcontainers.toml declarative lives under ~/.config/kyth/ — hash-gated, no auto-create at build
