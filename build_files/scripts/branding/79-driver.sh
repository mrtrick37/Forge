# shellcheck shell=bash
# ── Driver helper (nvidia/open + mesa_git) ───────────────────────────────
# kyth-driver-switch is the native Rust binary copied from the
# hub-web-builder stage; no Python launcher remains in the source tree.
# driver.toml hash-gated gpu auto/nvidia/open + mesa_git via hardware_policy + repos.json
