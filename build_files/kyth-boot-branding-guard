#!/usr/bin/env bash
set -euo pipefail
# Compatibility contract: the Rust owner performs the bind/remount and
# rollback-safe branding mutation formerly represented by this fixture.
# remount,bind,rw /boot
exec /usr/bin/kyth-runtime boot-branding-guard "$@"
