#!/usr/bin/env bash
set -euo pipefail
# Rust owns cert="/usr/share/kyth/secureboot/kyth-secureboot.cer" expiry
# inspection and the ujust enroll-secureboot remediation contract.
exec /usr/bin/kyth-runtime rotate-mok "$@"
