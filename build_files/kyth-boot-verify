#!/usr/bin/env bash
set -euo pipefail
# Rust owns the CERT="/usr/share/kyth/secureboot/kyth-secureboot.cer" check,
# the KERNEL_FLAVOR}" == "cachy" policy, and mismatch exit 2 contract.
exec /usr/bin/kyth-runtime boot-verify "$@"
